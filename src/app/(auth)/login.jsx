import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const login = () => {
  return (
    <SafeAreaView className="flex-1 bg-primary-50 p-4">
      <Text>Login</Text>
    </SafeAreaView>
  );
};

export default login;
